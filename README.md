# MVC

```bash
docker compose up --build
```

- Frontend: http://localhost:8081
- Middleware: http://localhost:8082/user?id=1
- Postgres: localhost:5432

El backend ya no se puede acceder desde fuera, ahora todo pasa por el middleware.
