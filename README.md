# MVC

```bash
docker compose up --build
```

- Frontend: http://localhost:8081
- Middleware: http://localhost:8082/user?id=1
- Postgres: localhost:5432 (app/app)

El backend ya no es accesible desde fuera; todo pasa por el middleware.
